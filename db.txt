--  select * from orders
-- select * from products
--  CREATE TABLE IF NOT EXISTS orders(
--  		id SERIAL PRIMARY KEY,
--  		firstname varchar(200) NOT NULL,
--  		lastname varchar(200),
--  		product_name varchar(200) not null,
--  		price FLOAT NOT  NULL,
--  		time TIMESTAMP NOT NULL
--  );
-- INSERT INTO orders(firstname, lastname, product_name, price, "time") values(1,'ali', 'valiyev', 'nok', 20, date_trunc('minute', NOW() AT TIME ZONE 'Asia/Tashkent'))
-- delete from orders where id=1
--  drop table orders
-- SELECT * FROM products WHERE id=1
-- DELETE FROM products
-- 	WHERE id = 1
	
	
-- 	IF FOUND THEN
-- 	  RAISE NOTICE 'Product  has been successfully deleted', <product_id>;
-- 	ELSE
-- 	  RAISE NOTICE 'Product  does not exist', <product_id>;
-- 	END IF;